<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Show Specific Post</title>
    <link rel="stylesheet" href="/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="bg-gray-900 text-white font-sans min-h-screen flex flex-col items-center justify-start py-6 sm:py-10 px-3"
  >
    <!-- Header -->
    <header class="mb-6 sm:mb-8 w-full max-w-md text-center">
      <h1 class="text-2xl sm:text-3xl font-bold text-purple-400 mb-2">
        Post Details ✨
      </h1>
      <p class="text-gray-400 text-sm sm:text-base">
        View this moment in full detail
      </p>
    </header>

    <!-- Post Card -->
    <main
      class="w-full max-w-md bg-gray-800 rounded-xl shadow-lg p-4 sm:p-6 border border-gray-700"
    >
      <!-- User Info -->
      <div class="flex items-center gap-3 mb-4">
        <img
          src="https://i.pravatar.cc/100?u=<%= post.user %>"
          alt="User Avatar"
          class="w-9 h-9 sm:w-10 sm:h-10 rounded-full border border-purple-400"
        />
        <div>
          <p class="font-semibold text-sm sm:text-base">@<%= post.user %></p>
          <p class="text-gray-400 text-xs sm:text-sm">
            📍 <%= post.location %>
          </p>
        </div>
      </div>

      <!-- Post Image -->
      <img
        src="<%= post.imageUrl %>"
        alt="Post Image"
        class="w-full max-h-[400px] sm:max-h-[500px] object-cover rounded-lg mb-4 border border-gray-600"
      />

      <!-- Likes & React -->
      <div
        class="flex items-center justify-between text-lg sm:text-xl mb-4 flex-wrap gap-2"
      >
        <div class="flex items-center gap-2 sm:gap-3">
          <button
            type="submit"
            class="hover:text-purple-400 transition"
            title="Like"
          >
            💜
          </button>
          <p class="font-semibold text-sm sm:text-lg">
            <%= post.likes %> likes
          </p>
        </div>
        <button
          class="hover:text-purple-400 transition text-lg sm:text-xl"
          title="React"
        >
          ✨
        </button>
      </div>

      <!-- Caption -->
      <div class="mb-2 text-sm sm:text-base">
        <p>
          <span class="font-semibold">@<%= post.user %></span> 💬 <%=
          post.caption %>
        </p>
      </div>

      <!-- Mood -->
      <div class="mb-2">
        <p class="text-gray-400 text-xs sm:text-sm">
          Mood: <%= post.mood || "🌸 Chill" %>
        </p>
      </div>

      <!-- Location -->
      <div class="mb-4">
        <p class="text-gray-400 text-xs sm:text-sm">
          Location: 📍 <%= post.location %>
        </p>
      </div>

      <!-- Edit / Back -->
      <div class="flex flex-col sm:flex-row justify-between gap-3 mt-4">
        <a
          href="/posts"
          class="bg-purple-600 hover:bg-purple-700 px-4 py-2 rounded-full shadow-md transition text-white font-medium text-center"
        >
          ⬅️ Back
        </a>
        <a
          href="/posts/<%= post.id %>/edit"
          class="bg-gray-700 hover:bg-gray-600 px-4 py-2 rounded-full shadow-md transition text-purple-400 font-medium text-center"
        >
          ✏️ Edit
        </a>
      </div>
    </main>
  </body>
</html>
